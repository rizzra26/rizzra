import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: "Projects - Rizky",
    description: "My projects"
}

const ProjectPage = () => {
    return (
        <div className='wrapper fade'>
            <h1 className="header">Coming Soon.</h1>
        </div>
    )
}

export default ProjectPage;